/**
 * Deprecated: the bookmarks table was merged into {@link BookmarkPanel} during the
 * teal card redesign. This module is retained only as a temporary re-export so the
 * build stays green; it is no longer referenced and should be deleted.
 */
export { default } from './BookmarkPanel';
